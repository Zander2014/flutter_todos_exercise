import 'package:flutter/material.dart';

///Created by apple on 2020/3/19.
class NetLoadingWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(''),
      ),
    );
  }
}